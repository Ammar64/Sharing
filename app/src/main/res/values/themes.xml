<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FilesCenter" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item tools:targetApi="l" name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:actionMenuTextColor">@color/brightTextColor</item>
    </style>

    <style name="Theme.FilesCenter" parent="Base.Theme.FilesCenter" />
</resources>