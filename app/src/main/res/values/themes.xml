<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FilesCenter" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item tools:targetApi="l" name="android:statusBarColor">#00000000</item>
        <item name="android:actionMenuTextColor">@color/brightTextColor</item>
        <item name="android:windowBackground">@drawable/gradient_background</item>
        <item name="android:fontFamily">@font/cairo</item>
    </style>

    <style name="Theme.FilesCenter" parent="Base.Theme.FilesCenter" />
    <style name="ShapeAppearanceOverlay.MyApp.Button.Circle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="ShapeAppearanceOverlay.MyApp.Button.Rounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>