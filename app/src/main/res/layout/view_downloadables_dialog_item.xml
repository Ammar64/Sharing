<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/TV_DownloadableDialogFileName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="16dp"
        android:textSize="18sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:maxEms="8"
        tools:text="Something txt" />

    <TextView
        android:id="@+id/TV_DownloadableDialogFileSize"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@+id/B_DownloadableDialogFileRemove"
        app:layout_constraintEnd_toStartOf="@+id/B_DownloadableDialogFileRemove"
        app:layout_constraintTop_toTopOf="@+id/B_DownloadableDialogFileRemove"
        android:layout_marginEnd="12dp"
        tools:text="(255MB)"
        />

    <android.widget.Button
        android:id="@+id/B_DownloadableDialogFileRemove"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:text="@string/remove"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>